
NumPy (Numerical Python) is a fundamental library for scientific computing in Python. 
It provides support for large, multi-dimensional arrays and matrices, along with a collection of high-level mathematical functions to operate on these arrays efficiently. 
It is the backbone of most data science and machine learning libraries like Pandas, Scikit-learn, and Matplotlib.
Key Commands and Operations Used
The following table summarizes the functions and attributes explored in this experiment:
np.array() Creates an array from a list or nested list.
np.zeros() Generates an array filled with zeros of a given shape.
np.ones() Generates an array filled with ones.
np.eye() Creates an identity matrix.
np.arange() Creates an array with a range of values (start, stop, step).
np.linspace() Generates evenly spaced numbers over a specified interval.Attributes.
ndimReturns the number of dimensions of the array..sizeReturns the total number of elements in the array..
shapeReturns a tuple representing the dimensions (rows, cols)..
dtypeReturns the data type of the elements in the array.Arithmetica + 5Scalar addition (adds 5 to every element).b * 2Scalar multiplication.Statisticsnp.
mean() Calculates the arithmetic mean.
np.median() Finds the median value.
np.sum() Computes the sum of all elements.
np.max() Finds the maximum values
np.min() Finds the maximum and minimum values.
